Step1: Install required libraries by using CMD command

pip install -r requirment.txt

Step2: after Sucessfull installation run this command

python3 FinalGCP.py

Note: Both commands should be run by changing the CMD directory to this file location


